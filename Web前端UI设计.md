# PerfDog-Like 性能监测平台 · Web 前端 UI 设计方案

> 本文档定义网站的产品原型与页面交互，覆盖从「设备连接」到「检测报告」的完整用户流程。
>
> **范围说明**：UI 原型按完整能力集合设计，但实际指标必须由设备/App 能力探测结果动态决定。Phase 1 默认稳定提供 App/System CPU、App PSS/RSS、System Memory、View/HWUI FPS、基础电池状态和 Logcat；GPU、Network、温度/电流、详细 Heap、GC、ANR/Crash 只有探测为 `available` 或 `degraded` 时才展示有效值。不可用指标必须显示原因，不能显示 0 或过期值。
>
> **运行边界**：浏览器不直接访问 ADB。页面通过本机 PC Agent 的 HTTP/JSON 和 WebSocket 操作设备、创建 Session、接收实时数据并查询报告；PC Agent 负责 ADB、采集、存储和报告生成。

---

## 一、整体页面结构

建议左侧固定导航：

```
┌──────────────────────────────────────────────────────────────┐
│ 性能监测平台                                      用户/设置 │
├────────────┬─────────────────────────────────────────────────┤
│            │                                                 │
│ 🏠 监测首页 │                                                 │
│            │                                                 │
│ 📱 设备管理 │             页面内容区域                        │
│            │                                                 │
│ 📊 实时监控 │                                                 │
│            │                                                 │
│ 📄 检测报告 │                                                 │
│            │                                                 │
│ ⚙️ 系统设置 │                                                 │
│            │                                                 │
└────────────┴─────────────────────────────────────────────────┘
```

其中最核心的导航链路：

```
监测首页 → 实时监控 → 检测报告
```

页面与 PC Agent 的最小交互顺序：

```text
加载页面
       -> GET /api/v1/devices
       -> GET /api/v1/devices/{deviceId}/applications
       -> POST /api/v1/devices/{deviceId}/capabilities
       -> POST /api/v1/sessions
       -> POST /api/v1/sessions/{sessionId}/start
       -> WebSocket /api/v1/sessions/{sessionId}/stream
       -> POST /api/v1/sessions/{sessionId}/stop
       -> GET /api/v1/sessions/{sessionId}/report
```

能力预检完成前，开始按钮保持禁用。开始后以服务端返回的实际启用指标为准，不以用户勾选状态作为能力保证。

---

## 二、首页：设备连接 + 创建监测任务

首页不要直接展示大量性能数据，而是负责回答一个问题：

> 「我准备监控哪个手机的哪个 App？」

```
┌─────────────────────────────────────────────────────────────┐
│ 性能监测                                                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ① 连接设备                                                   │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🟢 Google Pixel 10                                      │ │
│ │                                                         │ │
│ │ Android 17        USB                                  │ │
│ │ Serial: XXXXXXXX                                       │ │
│ │                                                         │ │
│ │ USB调试：已开启       ADB：已连接                       │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│                                                             │
│ ② 选择监测应用                                               │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🔍 搜索应用包名                                          │ │
│ ├─────────────────────────────────────────────────────────┤ │
│ │ ☑ com.yalla.hepsioyna                                  │ │
│ │   HepsiOyna                                             │ │
│ │   Version 3.9.1                                        │ │
│ │                                                         │ │
│ │   com.android.chrome                                   │ │
│ │   Chrome                                                │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│                                                             │
│ ③ 监测配置                                                   │
│                                                             │
│ 采样策略：CPU/System Memory 1s，PSS 2s，FPS 1s 窗口          │
│                                                             │
│ ☑ FPS                                                       │
│ ☑ CPU                                                       │
│ ☑ Memory                                                    │
│ ☑ Battery                                                   │
│ □ GPU（需设备支持）                                         │
│ □ Network（需设备与权限支持）                               │
│ □ Temperature/Current（按能力探测）                         │
│                                                             │
│                         [ 开始监测 ]                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、设备连接逻辑

设备连接状态建议做成非常明显的三态展示。

### 未连接

```
🔴 未检测到 Android 设备

请：
1. 使用 USB 连接手机
2. 开启开发者选项
3. 开启 USB 调试

              [重新检测]
```

### 检测到设备，但未授权

```
🟡 等待 USB 调试授权

请查看手机屏幕上的：
「是否允许此电脑进行 USB 调试？」

              [重新检测]
```

### 已连接

```
🟢 设备已连接

Pixel 10
Android 17
USB
ADB Connected
```

设备卡片还应显示 PC Agent 服务状态：`Agent 已连接`、`Agent 未启动` 或 `Agent 版本不兼容`。ADB 已连接但 Agent 不可用时，页面提供启动/重试提示，不显示可开始监测的状态。

### 无线 ADB 扩展

如果以后支持无线 ADB，可扩展连接方式切换：

```
连接方式

● USB
○ Wi-Fi ADB
```

---

## 四、选择包体

这个页面有一个非常重要的设计：**不要让用户手动输入 package name**，直接通过 ADB 获取：

```
adb shell pm list packages
```

网站展示：

```
搜索应用

┌──────────────────────────────────────┐
│ 🔍 HepsiOyna                         │
└──────────────────────────────────────┘

☑ HepsiOyna
  com.yalla.hepsioyna
  Version 3.9.1

○ Chrome
  com.android.chrome
  Version 140.0

○ YouTube
  com.google.android.youtube
  Version 21.x
```

建议增加筛选开关：

```
[仅显示当前运行中的 App]
```

这样测试人员会非常方便。

---

## 五、开始监测前的「任务确认」

点击「开始监测」后，不建议直接进入页面，而是弹一个确认框：

确认框打开前先完成目标 App 的能力预检，并展示每个指标的结果：`可用`、`降级`、`不支持` 或 `权限不足`。用户确认的是实际能力和采样策略，而不是未经验证的指标清单。

```
┌──────────────────────────────────────────┐
│        创建性能监测任务                  │
├──────────────────────────────────────────┤
│                                          │
│ 设备                                     │
│ Pixel 10                                 │
│ Android 17                               │
│                                          │
│ 应用                                     │
│ HepsiOyna                                │
│ com.yalla.hepsioyna                      │
│ Version 3.9.1                            │
│                                          │
│ 采样策略                                 │
│ CPU/System Memory 1s；PSS 2s；FPS 1s窗口 │
│                                          │
│ 监测指标                                 │
│ FPS / CPU / Memory / Battery              │
│                                          │
│              [取消] [开始监测]           │
└──────────────────────────────────────────┘
```

---

## 六、核心页面：实时监控

这是整个网站最重要的页面，采用「顶部状态 + 核心指标卡 + 实时曲线 + 详细指标」四段结构。

### 1. 顶部状态栏

```
┌─────────────────────────────────────────────────────────────┐
│ 🟢 正在监测                                                 │
│                                                             │
│ Pixel 10  │  HepsiOyna  │  00:13:42                        │
│                                                             │
│                     [暂停] [停止监测]                       │
└─────────────────────────────────────────────────────────────┘
```

可增加采样信息：

```
采样策略：CPU/System Memory 1s · PSS 2s · FPS 1s窗口
数据点：822
能力：7/10 指标可用 · 1项降级 · 2项不支持
```

---

## 七、第一屏核心指标

第一屏直接展示最重要的数据：

```
┌──────────────┬──────────────┬──────────────┬──────────────┐
│     FPS      │     CPU      │   Memory     │   Battery    │
│              │              │              │              │
│    59.8      │    42.3%     │    812 MB    │     82%      │
│              │              │              │              │
│  平均 58.6   │  平均 38.2%  │  平均 790MB  │   充电中     │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

再增加状态标识：

```
FPS       59.8      🟢
CPU       42.3%     🟢
Memory    812MB     🟢
GPU       不支持    ⚪
```

---

## 八、FPS 建议重点设计

因为这是游戏性能监控，**FPS 应该是第一核心指标**，做一个大型 FPS 卡片。卡片同时显示数据源和当前显示刷新率，不能假设所有设备都是 60Hz：

```
┌────────────────────────────────────────────────────┐
│ FPS                                                │
│                                                    │
│                 59.8 FPS                           │
│                                                    │
│  平均     最低      最高      P95                  │
│  58.6     31.2      60        60                   │
│                                                    │
│  🟢 流畅                                              │
└────────────────────────────────────────────────────┘
```

下面放实时曲线：

```
FPS
60 ┤ ───────────────────────╮──────╮──────
55 ┤                         ╰──────╯
50 ┤
45 ┤
40 ┤                    ╭────
35 ┤                 ╭──╯
30 ┤             ╭───╯
   └──────────────────────────────────────
     10s    20s    30s    40s    50s
```

---

## 九、性能曲线区域

建议**不要把所有指标画在一张图**，而是每个指标独立一张：

```
FPS
60 ┤────────────────────────────────────
55 ┤
50 ┤
45 ┤
40 ┤
35 ┤
30 ┤
   └────────────────────────────────────

CPU
CPU %
100┤
 80┤
 60┤             ╭────╮
 40┤─────────────╯    ╰──────────────
 20┤
  0┤
   └──────────────────────────────────

Memory
Memory MB
1000┤
 900┤                         ╭──────
 800┤─────────────────────────╯
 700┤
 600┤
    └────────────────────────────────
```

---

## 十、强烈建议增加「时间轴联动」

这个功能对性能分析非常有价值。用户点击某个时间点，联动查看该时刻所有指标：

```
FPS
60 ───────────────────────────
50 ───────────────╮───────────
40                │
30                ╰─────
                   ↑
                 12:31:22
```

点击后：

```
时间：12:31:22

FPS       31
CPU       87%
Memory    923MB
GPU       不支持
Network   不支持
```

这样测试人员可以直接发现：「FPS 突然掉到 31 的同时，CPU/GPU 发生了什么」，比单纯看几条曲线有用很多。

---

## 十一、增加「异常事件」区域

系统按第 18 节的规则实时判定异常。列表展示规则名、严重级和时间，不要只写「FPS 下降」这种无法复现判定条件的文案：

```
异常事件

🔴 12:31:22  FPS骤降  58.6→31.2  持续3.2s
🟠 12:32:10  CPU持续高  ≥85%  持续8s
🟠 12:33:42  内存骤增  +192MB  打开商城
🔴 12:35:01  持续低帧  <30FPS  持续3s
ℹ️ 12:34:08  内存骤降  -183MB  疑似GC
```

点击事件：

以下示例假设 GPU 能力探测结果为 `available`；若不可用，显示“不支持”和具体原因。

```
12:31:22 FPS骤降  FPS_SUDDEN_DROP #02

FPS       58.6 → 31.2
CPU       42% → 91%
GPU       45% → 87%
Memory    812 → 923MB
Jank      11 / BigJank 3

复合命中：计算尖峰卡顿 + 渲染尖峰卡顿

[查看时间点]
```

点击以后，所有曲线跳到这个时间点。实时列表与报告共用同一套规则 ID、合并窗口和严重级，停止监测后不重新发明另一套统计。

---

## 12. 详细指标 Tab

实时监控页面下面可以做 Tab 切换：

```
[概览] [CPU] [Memory] [FPS] [GPU] [Network] [Battery] [日志]
```

点击 CPU 示例。每个指标页顶部都要显示 `source`、采样周期和 `quality`，让用户知道这是精确值、降级值还是不支持：

```
CPU监控

App CPU             42.3%
System CPU          18.2%

CPU Core

CPU0    31%
CPU1    42%
CPU2    61%
CPU3    39%
CPU4    48%
CPU5    32%
CPU6    27%
CPU7    25%

────────────────────────────

CPU实时曲线
```

---

## 13. Memory 页面

建议不要只显示 `Memory = 800MB`，而是拆开；只有设备能力探测支持的字段才显示，其他字段显示“不支持/需 SDK”，不要填 0：

```
Memory

App Memory        812 MB
PSS               790 MB
Private Dirty     需能力支持
Native Heap       230 MB
Java Heap         150 MB
Graphics          不支持

────────────────────────

Memory趋势

812MB ──────────────────╮
                        ╰──── 920MB

────────────────────────

GC

GC次数：需 Target App SDK
GC耗时：需 Target App SDK
```

后续如能获取，还可加入：Private Dirty、Graphics、Stack、Code、Other；每项必须记录来源和质量状态。

---

## 14. Network 页面

> ⚠️ 采集范围备注：Network 是能力驱动指标。若 UID 流量或系统接口不可用，页面保留入口但显示“不支持及原因”，不展示伪造速率；Phase 1 不把它列为默认必选指标。

```
Network

↑ Upload       52 KB/s
↓ Download     180 KB/s

Total Upload   12.2 MB
Total Download 48.6 MB

────────────────────────

实时网络流量

Download
     ╭────╮
─────╯    ╰────────

Upload
────────╮
        ╰──────────
```

还可增加：网络请求次数、TCP 连接、UDP 流量、丢包、延迟。

---

## 15. 监测结束

点击「停止监测」后弹出确认框：

停止后 Session 进入 `finalizing`，前端继续显示收尾进度和已落盘数据量；报告生成完成后才允许查看详情和导出。浏览器刷新或 WebSocket 断开不应导致采集中断。

```
┌──────────────────────────────────────────┐
│           结束本次监测？                 │
├──────────────────────────────────────────┤
│                                          │
│ 监测时长：32分18秒                       │
│ 数据点：1938                             │
│                                          │
│ FPS平均：57.8                            │
│ FPS最低：27.2                            │
│ CPU平均：42.3%                           │
│ Memory平均：812MB                        │
│                                          │
│ ☑ 保存监测数据                           │
│ ☑ 自动生成检测报告                       │
│                                          │
│             [取消] [结束监测]            │
└──────────────────────────────────────────┘
```

---

## 16. 检测报告页面

报告列表：

```
检测报告

┌─────────────────────────────────────────────────────┐
│ HepsiOyna 性能测试                                   │
│ Pixel 10 / Android 17                               │
│ 2026-09-04 10:20                                    │
│                                                     │
│ 测试时长：32m18s                                    │
│ FPS平均：57.8                                       │
│ FPS最低：27.2                                       │
│ CPU平均：42.3%                                      │
│ Memory平均：812MB                                   │
│                                                     │
│ [查看报告] [导出 PDF] [导出 Excel]                  │
└─────────────────────────────────────────────────────┘
```

---

## 17. 报告详情

报告不要只是把实时监控页面截图下来，建议自动生成结构化的报告。

### ① 测试基本信息

```
测试报告

应用：HepsiOyna
Package：com.yalla.hepsioyna
Version：3.9.1

设备：Pixel 10
Android：17
测试时间：2026-09-04 10:20
测试时长：32m18s
能力覆盖：7/10 指标可用，1项降级，2项不支持
```

### ② 性能结论

```
性能概览

FPS       57.8       🟢 良好
CPU       42.3%      🟢 良好
Memory    812MB      🟡 注意
GPU       51.2%      🟢 良好（能力可用时）
```

自动生成测试结论：

> 本次测试持续32分钟18秒。
>
> 期间平均FPS为57.8，整体运行稳定。
> 检测到 2 次 FPS骤降、1 次持续低帧；其中 #02 与 CPU/GPU 同时打满重合，偏瞬时算力峰值。
> Memory 在测试后期按 2.4MB/min 持续爬升，最高 1.12GB，命中内存泄漏观察，建议结合「打开商城」之后的区间复核。

---

## 18. 报告最有价值的是「异常分析」

异常分析不要做成「某个采样点越过阈值就算一次」。一次采样噪声很大（CPU 1s、PSS 2s、FPS 1s 窗口），「骤增 / 骤降」必须相对一段基线，并同时满足变化量、落点、确认时长，再用合并窗口把碎事件收成一次。

实时监控的异常列表和报告详情消费同一套结构化结果，展示层不得按另一套阈值重算。

### ① 检测原语

每条规则固定 6 个字段：

| 字段 | 含义 | Phase 1 默认 |
| --- | --- | --- |
| 基线窗口 | 取「正常值」 | 过去 10s 中位数（抗尖刺，不用均值） |
| 触发条件 | 绝对值 / 相对跳变 / 斜率 | 「跳了多少」和「落到哪里」必须同时满足 |
| 确认时长 | 过滤单点噪声 | 至少 2～3 个有效采样点 |
| 合并窗口 | 中间短暂恢复仍算同一次 | 恢复不足 3s 不拆事件 |
| 严重级 | `info` / `warn` / `critical` | 明显影响体验才 `critical` |
| 关联切片 | 异常点前后其它指标 | 前后各 5s；GPU / Network 仅 `available` 才带 |

刷新率不要写死 60Hz。流畅线 `0.95 × refreshHz`，可玩线 `0.5 × refreshHz`，严重线取 `min(30, Hz/2)`。CPU「骤增百分之多少」一律用 **百分点（pp）**：App CPU 20%→26% 是相对涨 30%，对游戏无意义；应写成「3 秒内升高 25 个百分点，且超过 70%」。内存才允许用相对涨幅，因为 500MB 和 1.5GB 的 App 绝对增量差一截。

`unavailable` / `degraded` 样本既不当基线，也不当触发。启动后 15s 对 CPU/内存规则静默，避免进程拉起、JIT、预加载打爆列表。用户打了场景标记时，该时刻 ±3s 的 `MEM_SPIKE` / `CPU_SPIKE` 降为 `info`，或打标签 `expected-on-scene-enter`。

### ② 默认规则

| 规则 ID | 场景 | 触发（默认） | 确认 / 合并 | 严重级 |
| --- | --- | --- | --- | --- |
| `FPS_SUDDEN_DROP` | FPS 骤降 | 相对过去 10s 中位掉 `max(15, 0.25×Hz)`，且当前 `< 0.7×Hz` | 连续 ≥2s；间隔 <3s 合并 | 最低 FPS ≥ 0.5×Hz 为 warn，否则 critical |
| `FPS_SUSTAINED_LOW` | 持续低帧 | FPS `< 0.5×Hz`（60Hz 即 <30） | 连续 ≥3s；回升不足 5s 不拆 | critical |
| `JANK_BURST` | Jank 簇 | 严格：1s 内 Jank ≥3 或出现 1 次 BigJank；降级：1s 窗口 FPS `< 0.5×Hz` | 间隔 <2s 合并 | 有 BigJank → critical，否则 warn |
| `CPU_SPIKE` | CPU 骤增 | App CPU 相对基线 `+25pp` 且当前 ≥70% | 连续 ≥3s；间隔 <5s 合并 | 峰值 <85% 或持续 <5s → warn；≥85% 且持续 ≥8s → critical |
| `CPU_HIGH` | CPU 持续高 | App CPU ≥85% | 连续 ≥8s | critical |
| `CPU_CORE_HOTSPOT` | 单核打满 | 有 per-core 时：任一核 ≥95%，其余核平均 <40% | 连续 ≥5s | warn |
| `MEM_SPIKE` | 内存骤增 | 4s 内 PSS +80MB，或相对基线 +15% 且绝对增量 ≥50MB | 新水位维持 ≥4s | warn；同时 PSS >1GB → critical |
| `MEM_DROP_GC` | 内存骤降 | 4s 内 PSS -80MB 或 -15% | — | 默认 **info**，进「内存事件」不算异常次数；仅当同秒 FPS <30 才升为 warn |
| `MEM_LEAK_TREND` | 内存泄漏观察 | 最近 5 分钟斜率 ≥2MB/min，期间无 ≥50MB 回落，终点比起点高 ≥80MB | 至少 10 个有效 PSS 点 | warn；斜率 ≥5MB/min 或峰值 >1GB → critical |
| `MEM_HIGH` | 内存封顶 | PSS >1GB | 维持 ≥4s | critical |

PSS 不可用时降级 RSS，并标记 `quality=degraded`。Jank 有逐帧 FrameTime 时用 `perfdog-display-frametime-v1`；只有窗口 FPS 时 `quality=estimated`，报告必须分开，不得把 Jank 与 BigJank 相加。

骤降和低帧必须拆开：30 分钟一直 28 帧应记为 **1 次持续低帧**，不要拆成几十次骤降。

### ③ 复合异常（有分析价值的部分）

单指标只回答「发生了什么」。复合规则在关联指标均为 `available` 时启用，用来给 QA 一句假设。没有 GPU 时仍可报计算尖峰，但文案必须写「GPU 不支持，无法排除渲染瓶颈」。

| 场景名 | 组合条件 | 给 QA 的假设 |
| --- | --- | --- |
| 计算尖峰卡顿 | FPS 骤降，且同 2s 内 App CPU +25pp | 逻辑 / AI / 物理峰值 |
| 渲染尖峰卡顿 | FPS 骤降，且 GPU ≥85% | 特效 / 合批 / Overdraw |
| 加载卡顿 | FPS 骤降，且 PSS 4s 内 +80MB | 进房 / 开商城 / 切场景加载 |
| GC 卡顿 | FPS <30 持续 ≥1s，且 PSS 骤降 ≥80MB | 疑似 GC；Phase 1 无 GC 计数时文案写「疑似」 |
| 热节流 | 温度 60s 内 +8℃，且 FPS P95 下降 ≥10，且 CPU 反而下降 | 降频，不是代码突然变慢 |
| 网络卡死 | Network 能力可用：对局中下行 ≈0 连续 5s | 弱网 / 超时，不一定是性能 |

### ④ 异常统计（按规则分桶，不要只按大类）

```
异常统计

FPS骤降           2次
持续低帧          1次（累计 18s）
Jank簇            4次（BigJank 3）
CPU骤增           1次
CPU持续高         1次
内存骤增          1次
内存泄漏观察      1次（2.4MB/min，峰值 1.12GB）
ANR               0次
Crash             0次

内存事件（不计入异常次数）
疑似GC/释放       1次
```

### ⑤ 单条异常卡片

点击 `FPS骤降 #02`：

```
异常 FPS_SUDDEN_DROP #02          critical
时间     12:31:22 ～ 12:31:25（3.2s）
场景     进入 Okey Classic 对局（标记 05:12）
规则     基线 10s 中位 58.6 → 最低 31.2，连续 2s，刷新率 60Hz

FPS
60 ─────────────╲
                 ╲
30                ╰──────
                  ↑
               异常点

主指标   FPS  58.6 → 31.2
关联     CPU  42→91%   GPU 45→87%   PSS 812→923MB   Jank 11 / BigJank 3
复合命中 计算尖峰卡顿 + 渲染尖峰卡顿
结论     同秒 CPU/GPU 一起打满，偏瞬时算力峰值；内存只有 +111MB，不像泄漏。

[查看时间点]   [前后 10s 曲线]
```

GPU 不可用时该行显示「不支持」和原因，不填 0，也不参与「渲染尖峰卡顿」判定。

内存骤增示例：

```
异常 MEM_SPIKE #01                warn
时间     12:33:40 ～ 12:33:46（6s）
场景     打开商城（标记 18:32）
规则     4s 内 PSS +192MB（+24%），新水位维持 6s

PSS      812 → 1004 MB
Java     150 → 210 MB
Native   230 → 318 MB
FPS      59 → 47（轻微伴随掉帧）
结论     资源集中加载。若之后 5 分钟没有回落，由 MEM_LEAK_TREND 升级为泄漏观察。
```

内存骤降默认不当故障：

```
内存事件 MEM_DROP_GC              info → 因同秒掉帧升为 warn
时间     12:34:08
PSS      1004 → 821 MB（-183 MB）
同秒 FPS 58 → 29，持续 1s
结论     疑似 GC stop-the-world。Phase 1 无 GC 计数，仅能推断。
```

### ⑥ 设置页可调旋钮

设置里的 FPS / CPU / Memory 阈值不要让用户写公式，只暴露：

```
异常检测

基线窗口          10s
确认时长          FPS 2s / CPU 3s / Memory 4s
合并窗口          3s

FPS 骤降          掉 max(15, 0.25×Hz) 且 <0.7×Hz
持续低帧          <0.5×Hz，持续 3s
CPU 骤增          +25pp 且 ≥70%
CPU 持续高        ≥85%，持续 8s
内存骤增          4s +80MB 或 +15%（至少 +50MB）
内存封顶          PSS > 1GB
泄漏斜率          ≥2MB/min（5 分钟窗口）
启动静默          15s
```

这样报告才真正具备分析价值：能回答发生了哪类异常、持续多久、和哪些指标一起变、更像加载/算力/GC 还是泄漏。

---

## 19. 建议加入「测试场景」

如果工具主要给 QA 使用，不要只记录「这个 App 测了 30 分钟」，而是让测试人员可以标记场景。

```
测试场景

[+ 添加场景]

00:00  启动游戏
02:30  进入大厅
05:12  进入Okey Classic
08:30  开始对局
15:20  发送语音
18:32  打开商城
22:10  进入下一局
30:20  退出游戏
```

实时监控时提供「添加标记」：

```
[+ 添加标记]
```

点击后：

```
添加测试标记

名称：
[进入Okey Classic________]

备注：
[开始正式测试____________]

                    [确定]
```

报告里即可与场景标记对应起来，对 QA 实际使用非常方便。进入对局、打开商城等标记前后 ±3s 的 CPU/内存骤增按第 18 节打 `expected-on-scene-enter`，避免把切场景加载算成故障。

---

## 20. 整体用户操作流程

把整个产品流程串起来：

```
                    ┌──────────────┐
                    │    首页      │
                    └──────┬───────┘
                           ↓
                    检测 Android 设备
                           ↓
                    ┌──────────────┐
                    │ 设备已连接   │
                    └──────┬───────┘
                           ↓
                    获取已安装 App
                           ↓
                    选择 Package
                           ↓
                    配置监测指标
                           ↓
                    ┌──────────────┐
                    │ 开始监测     │
                    └──────┬───────┘
                           ↓
             ┌─────────────┴─────────────┐
             ↓                           ↓
        实时数据采集                  异常检测
             ↓                           ↓
       FPS/CPU/Memory              异常事件记录
       GPU/Network等
             └─────────────┬─────────────┘
                           ↓
                       停止监测
                           ↓
                    保存监测数据
                           ↓
                    自动生成报告
                           ↓
             ┌─────────────┴─────────────┐
             ↓                           ↓
          在线查看                    导出报告
                                      PDF/Excel
```

---

## 21. 网站最终菜单

可直接按此做产品原型：

```
性能监测平台
│
├── 🏠 监测首页
│   ├── 设备连接
│   ├── App选择
│   └── 创建监测
│
├── 📱 设备管理
│   ├── 当前设备
│   ├── 已连接设备
│   └── 设备信息
│
├── 📊 实时监控
│   ├── 概览
│   ├── FPS
│   ├── CPU
│   ├── Memory
│   ├── GPU
│   ├── Network
│   ├── Battery
│   └── 异常事件
│
├── 📄 检测报告
│   ├── 报告列表
│   ├── 报告详情
│   ├── PDF
│   └── Excel
│
└── ⚙️ 设置
       ├── 采样策略
       ├── 异常检测（基线窗口 / 确认时长 / 合并窗口）
       ├── FPS阈值（骤降幅度、持续低帧）
       ├── CPU阈值（骤增百分点、持续高）
       ├── Memory阈值（骤增、封顶、泄漏斜率）
       └── 报告模板
```

---

## 附：页面清单总览

| # | 页面 / 模块 | 关键设计点 |
|---|---|---|
| 1 | 整体结构 | 左侧固定导航，核心链路「首页 → 实时监控 → 报告」 |
| 2 | 监测首页 | 设备连接 + App 选择 + 监测配置三步骤 |
| 3 | 设备连接 | 三态展示（未连接 / 待授权 / 已连接），预留 Wi-Fi ADB |
| 4 | 选择包体 | `pm list packages` 自动获取，禁止手输包名，支持「仅显示运行中 App」 |
| 5 | 任务确认 | 开始前弹确认框，汇总设备/应用/频率/指标 |
| 6 | 实时监控 | 顶部状态 + 能力驱动指标卡 + 曲线 + 详细指标四段式 |
| 7 | 核心指标卡 | FPS / CPU / Memory / Battery 当前值 + 平均值 + 状态灯；GPU 不可用时显示能力状态 |
| 8 | FPS 大卡片 | 平均/最低/最高/P95 + 流畅度评级 |
| 9 | 性能曲线 | 每指标独立成图，不堆叠 |
| 10 | 时间轴联动 | 点击时间点联动所有指标 |
| 11 | 异常事件 | 自动判定 + 点击跳转时间点 |
| 12 | 详细 Tab | 概览/CPU/Memory/FPS/GPU/Network/Battery/日志 |
| 13 | Memory 页 | 按能力拆 PSS/Private Dirty/Native/Java/Graphics；GC 默认由 SDK 提供 |
| 14 | Network 页 | 上下行速率 + 累计流量（Phase 1 受限） |
| 15 | 监测结束 | 结束确认 + 汇总 + 保存/生成报告选项 |
| 16 | 报告列表 | 卡片式 + 导出 PDF/Excel |
| 17 | 报告详情 | 基本信息 + 性能结论（自动生成文字结论） |
| 18 | 异常分析 | 异常统计 + 单次异常时间点 + 关联指标变化 |
| 19 | 测试场景 | 场景/标记功能，QA 友好 |
| 20 | 操作流程 | 端到端流程（连接 → 采集 → 报告 → 导出） |
| 21 | 菜单结构 | 五级导航的完整产品骨架 |
